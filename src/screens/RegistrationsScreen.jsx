import React from 'react'
import Registrations from '../components/auth/Registrations/Registrations';

const RegistrationsScreen = (props) => (
    <Registrations {...props} />
);

export default RegistrationsScreen;
